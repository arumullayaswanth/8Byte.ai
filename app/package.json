{
  "name": "8byte-sample-app",
  "version": "1.0.0",
  "description": "Sample Express API with health, metrics and a db-backed endpoint.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage --coverageReporters=lcov --coverageReporters=text",
    "lint": "eslint src"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
